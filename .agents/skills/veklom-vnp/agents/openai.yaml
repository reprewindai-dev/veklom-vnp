interface:
  display_name: "Veklom Vnp"
  short_description: "Repo-specific patterns and workflows for veklom-vnp"
  default_prompt: "Use the veklom-vnp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true